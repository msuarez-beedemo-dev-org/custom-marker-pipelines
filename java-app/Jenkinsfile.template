pipeline {
  parameters {
    string(defaultValue: "@completed", description: 'Cucumber Tag - Default is @completed', name: 'CUCUMBER_TAG')
  }
  agent { label 'java-app' }
  stages {
    stage('Test') {

      steps {
     container('maven') {
        sh "mvn test -Dcucumber.options=--tags "${params.CUCUMBER_TAG}""
        }
      }
    }
  }
}
